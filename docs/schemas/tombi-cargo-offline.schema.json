{
  "title": "Cargo manifest validated semantically by Cargo",
  "description": "Offline Tombi fallback that intentionally validates only the manifest root shape.",
  "type": "object",
  "additionalProperties": true
}
